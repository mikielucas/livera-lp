{
  "name": "livera-landing-page",
  "version": "1.0.0",
  "description": "Livera landing page with optimized images",
  "scripts": {
    "optimize-images": "node scripts/make-images.mjs",
    "dev": "python3 -m http.server 8000",
    "build": "echo 'Build complete'"
  },
  "devDependencies": {
    "sharp": "^0.33.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
